# express-api-boilerplate
1) idempotency
2) recilient
3) fault tolerent 
4) data validator
5) api version

# Get Started
1) clone repository 
2) `npm install`
3) `npm start`

# Tasks in queue

1) Move ES5 to ES6
2) add redis dependancy to manage recent transation data
